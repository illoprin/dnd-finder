services:
  nginx-certbot:
    image: nginx:alpine
    container_name: dndfinder_nginx_certbot
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.certbot.conf:/etc/nginx/conf.d/default.conf
      - /var/www/certbot:/var/www/certbot